// string is a collection of characters
console.log("hello javascript learner with shalu");


// the length property represents the length of string

var fruits = "pomegranate"
console.log(fruits.length); // returns 11

var empty = " ";
// it returns 1 because this string has a space.
console.log(empty.length);

var nothing = '';
console.log(nothing.length); // it returns 0
