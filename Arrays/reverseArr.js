
// The reverse() method reverse an array in place. The first array element becomes the last ,and the last element array
// becomes the first.

var sequence = ['one','two','three'];
console.log(sequence.reverse());//returns [ 'three', 'two', 'one' ]
console.log(sequence);
